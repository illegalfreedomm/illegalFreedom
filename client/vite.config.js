import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// vite.config.js
export default {
  build: {
    outDir: 'build', // Change this to "build" to match Vercel's expectations
  }
};
